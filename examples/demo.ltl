// examples/demo.ltl — runnable demo for ltl_cbor

import std::io
use ltl_cbor as lib

fn main() {
    io::println("ltl_cbor demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}